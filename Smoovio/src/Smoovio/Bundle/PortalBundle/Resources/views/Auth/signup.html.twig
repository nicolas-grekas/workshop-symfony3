{% extends 'SmoovioPortalBundle::layout.html.twig' %}

{% block content %}
    <h1>Sign up !</h1>
    <form action="{{ path('auth_signup') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
    </form>
{% endblock %}
